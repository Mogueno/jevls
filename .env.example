# TypeSafe Jev API Key (required for code analysis)
# Obtain your key from TypeSafe (https://typesafe.ai)
TYPESAFE_API_KEY=

# The waitlist database is Cloudflare D1, bound to the Worker as `DB`
# (see wrangler.jsonc) — no connection string or external service needed.
# Local dev gets a local D1 automatically.
